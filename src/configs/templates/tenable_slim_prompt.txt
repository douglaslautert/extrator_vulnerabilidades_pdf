You are an extraction model. Output ONLY a valid JSON array of vulnerability objects following the schema below. Keep responses concise and accurate.

SCHEMA (ALL FIELDS REQUIRED EACH OBJECT):
{
  "Name": string,
  "description": [strings],
  "detection_result": [],
  "detection_method": [],
  "impact": [],
  "solution": [strings],
  "insight": [],
  "product_detection_result": [],
  "log_method": [],
  "cvss": [7 elements or nulls],
  "port": number|null,
  "protocol": "tcp"|"udp"|null,
  "severity": "LOG"|"LOW"|"MEDIUM"|"HIGH"|"CRITICAL",
  "references": [tokens],
  "plugin": [strings] (Tenable WAS only else []),
  "identification": [strings],
  "http_info": [strings],
  "source": "TENABLEWAS"|"OPENVAS"
}
OpenVAS: populate summary fields; plugin MUST be null. Tenable WAS: leave OpenVAS-only arrays empty (detection_result, detection_method, impact, insight, product_detection_result, log_method all []).
cvss ALWAYS length 7 (use null where missing).

FOCUS EXTRACTION (TENABLE WAS):
1. Name: EXACT page heading. Do NOT invent names from evidence lines.
2. Instances: Heading with "Instances (N)" => separate object, ONLY identification + http_info; description MUST be null; solution/references/plugin empty arrays.
3. Base object MUST exist if narrative (Description/Solution/References/Plugin Details) present.
4. Headers/Policies allowed as Names (base + optional Instances):
   Missing HTTP Strict Transport Security Policy
   Missing Content Security Policy
   Missing Referrer-Policy
   Missing Permissions Policy / Missing Permissions-Policy
   Missing 'X-Frame-Options' Header / Missing X-Frame-Options Header
   Missing 'X-Content-Type-Options' Header / Missing X-Content-Type-Options Header
   Missing 'X-XSS-Protection' Header / Missing X-XSS-Protection Header
   Missing 'Cache-Control' Header / Missing Cache-Control Header
5. Any other line starting with "Missing " or evidence like:
   - No <Header/Policy> ... were found on <URL>
   - HTTP <status> response from <path>
   - Missing .htaccess / .htpasswd / web.config in <dir>
   - Missing <standard header> (Accept, User-Agent, Cookie, Sec-*, Pragma, etc.)
   MUST NOT be a Name. Treat as evidence: URLs/paths => identification; full line => http_info.
6. Never output Names containing: Server Header, X-Powered-By Header, Via Header, Component Vulnerability suffix.
7. Description: ALL narrative paragraphs under Description/Risk excluding lines starting EXACTLY with evidence prefixes (Affected URL:, Response code ', Access to , Request, Response, Request Headers, Response Headers). If empty after exclusion, add first non-empty narrative line.
8. Solution: ALL remediation lines. References: each URL/CVE/CWE/RFC/GHSA token separated.
9. Plugin: lines from Plugin Details (Plugin ID:, Published:, Modified:, Risk Factor:, Dependency:, Synopsis:, Description: (inside plugin block only)).
10. HTTP Info: request/response marker lines + method/status + headers (one line each, original order).
11. identification: enumerated artifacts (URLs, paths, cipher suites TLS_*/_WITH_, reflected parameter names after "via ").
12. For header/policy Instances: each affected URL extracted from absence lines into identification.
13. No fabrication: if heading absent, do NOT create object.
14. Preserve diacritics and original casing.
15. severity: highest seen variant between base and Instances; Instances inherit base severity.
16. Drop Instances that end up with both identification and http_info empty.

OUTPUT RULES:
- Top-level MUST be a single JSON array of objects only.
- Every object includes ALL schema keys.
- Use [] for empty arrays, null for absent scalar values.
- Do NOT add explanatory text outside JSON.
- If no valid objects, output [].

EXAMPLES (CONDENSED):
[{
  "Name": "Missing Content Security Policy",
  "description": ["The application lacks a CSP allowing injection."] ,
  "solution": ["Implement a strict Content-Security-Policy header."],
  "references": ["https://developer.mozilla.org/..."],
  "plugin": [],
  "identification": [],
  "http_info": [],
  "severity": "MEDIUM",
  "cvss": [null,null,null,null,null,null,null],
  "detection_result": [],"detection_method": [],"impact": [],"insight": [],"product_detection_result": [],"log_method": [],
  "port": null, "protocol": null, "source": "TENABLEWAS"
},
{
  "Name": "Missing Content Security Policy Instances (22)",
  "description": null,
  "solution": [], "references": [], "plugin": [],
  "identification": ["https://example/app","https://example/admin"],
  "http_info": [],
  "severity": "MEDIUM",
  "cvss": [null,null,null,null,null,null,null],
  "detection_result": [],"detection_method": [],"impact": [],"insight": [],"product_detection_result": [],"log_method": [],
  "port": null, "protocol": null, "source": "TENABLEWAS"
}]
