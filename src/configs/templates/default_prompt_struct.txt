


You are an information extraction model.

Your task is to extract structured vulnerability information from the TEXT REPORT provided above.


Return the output as a list of vulnerabilities, where each vulnerability is presented in plain text, one after the other, and each field is clearly labeled as below (always include all fields, even if the value is missing or null). Separate each vulnerability with a line containing only three dashes (---):


---
Name: <vulnerability name as found in the report or null>
Description: <detailed full description of the vulnerability as found in the report or null>
Detection Result: <detailed full detection result of the vulnerability as found in the report OPENVAS only or null>
Detection Method: <detailed full detection method of the vulnerability as found in the report OPENVAS only or null>
Impact: <detailed full impact of the vulnerability as found in the report OPENVAS only or null>
Solution: <list of recommended action or fix verbatim from the report or null>
Insight: <detailed full insight of the vulnerability as found in the report OPENVAS only or null>
Product Detection Result: <detailed full product detection result of the vulnerability as found in the report OPENVAS only or null>
CVSS: <all CVSS scores found, or null>
Port: <number or null>
Protocol: <protocol or null>
Severity: <LOG|LOW|MEDIUM|HIGH|CRITICAL|NULL>
References: <all references of the vulnerability as found in the report or null>
Plugin: <all plugin detail of the vulnerability as found in the report or null>
Source: <OPENVAS|TENABLEWAS|null>

...and so on for each vulnerability found.

For every vulnerability found, always output all fields above, even if some values are missing or not present in the report (use null or leave blank in these cases). Never omit any field.

**SECTION A — OPENVAS REPORTS** 
For reports generated by OpenVAS:
1. For each "NVT" is a block of vulnerability, extract a JSON object using the fields above.
2. Use "NVT" field as "Name".
3. Use "Summary" field as "description". 
4. Use "Vulnerability Detection Result" as "detection_result".
5. Use "Vulnerability Detection Method" as "detection_method".
6. Use "Product Detection Result" as "product_detection_result".
7. Use "Impact" field as "impact".
8. Use "Solution" field as "solution".
9. Use "Vulnerability insight" field as "insight".
10. Use "Product Detection Result" as "product_detection_result".
11. Use "CVSS" field as unique "cvss".
12. Get "port" and "protocol": Extract from above the CVSS.
13. Use "References" field as "references".
14. plugin is null. 
15. "severity" only use if found in report like LOW|MEDIUM|HIGH|CRITICAL, otherwise LOG.
16. Use "Log Method" field as "log_method".

**SECTION B — TENABLE WAS REPORTS**  
For reports generated by Tenable WAS:
1. For each "Vulnerability" is a block of vulnerability, extract a JSON object using the fields above.
2. Use "Vulnerability" field as "Name".
3. Use "Description" field as "description".  
4. Use "Solution" field as "solution".
5. Use "References Information" field as "references".
6. Use "Plugin Detail" field as "plugin".
7. For "detection_result" is null.
8. For "detection_method" is null.
9. For "impact" is null.
10. For "insight" is null.
11. For "product_detection_result" is null.
12. For "log_method" is null.

**GENERAL INSTRUCTIONS**
1. Extract every occurrence of a vulnerability, even if the name is repeated. Do not group, merge, or skip any block.